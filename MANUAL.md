# 操作マニュアル（詳細版）

このファイルは [README](README.md) の「使い方／キーバインド／ビルトイン」を、挙動・理由・例まで含めて詳しく書き下したものです。README は要約、こちらが全機能の参照先です。「どんな機能があったか忘れた」ときはここを読み返してください。

---

## 目次
- [起動と終了](#起動と終了)
- [コマンド実行モデル](#コマンド実行モデル)
- [プロンプトの見方](#プロンプトの見方)
- [キーバインド](#キーバインド)
- [Tab 補完](#tab-補完)
- [ゴースト補完](#ゴースト補完)
- [ピッカー（Ctrl+R / Ctrl+T / Ctrl+G）](#ピッカーctrlr--ctrlt--ctrlg)
- [パスの自動記録](#パスの自動記録)
- [ビルトインコマンド](#ビルトインコマンド)
- [abbr と alias の違い](#abbr-と-alias-の違い)
- [展開](#展開)
- [設定ファイル](#設定ファイル)
- [シグナル / Ctrl+C](#シグナル--ctrlc)
- [ジョブ制御](#ジョブ制御)
- [設計方針：あえて入れていない機能](#設計方針あえて入れていない機能)

---

## 起動と終了

```sh
cargo run --release
```

- 起動時に `~/.my_shell_rc` を 1 行ずつ実行する（[設定ファイル](#設定ファイル)）。
- 終了は空行で `Ctrl+D`。入力がある状態の `Ctrl+D` は「カーソル位置の 1 文字削除」になる（fish と同じ）。

---

## コマンド実行モデル

入力行は次の順で処理される。

1. 先頭トークンの **abbr** を 1 段だけ展開する（abbr は先頭トークンのみ対象）。
2. ビルトイン判定のため **alias** も 1 段展開してコマンド名を求める。
3. コマンド名が[ビルトイン](#ビルトインコマンド)なら本体で実行する（`sh` を経由しない）。引数の `~` / `$VAR` は本体側で展開する。
4. ビルトインでなければ、alias 定義をすべて前置したうえで行を **そのまま `sh -c` に渡す**。
   - パイプ・リダイレクト・グロブ・`$VAR`・クォートは **すべて `sh` が解釈**する。
     例: `ls *.rs | grep main > out.txt` がそのまま動く。
   - alias はパイプの後（`ls | grep` の `grep` など、コマンド位置すべて）でも展開される。
   - 直前コマンドの終了ステータスは先頭の `(exit N)` で `$?` に引き継がれる（`sh -c` は毎回新しいシェルなので明示している）。

---

## プロンプトの見方

ヘッダ行と入力行の 2 行で表示される。配色は iceberg（dark）系の落ち着いたトーン。

```
user@host ~/projects/foo (main)
$ コマンド…
```

| 要素 | 色 | 意味 |
| --- | --- | --- |
| `user@host` | blue | ユーザ名@ホスト名 |
| カレントディレクトリ | cyan | `~` 省略つきの cwd |
| `(branch)` | purple | git ブランチ（git リポジトリ内のときだけ表示） |
| `$` | **緑 / 赤** | **直前コマンドの終了ステータス**。成功(0)=緑、失敗(非0)=赤 |
| ゴースト補完 | 暗いグレー | 行末に薄く出る履歴ベースの候補 |

`$` の色を見れば、直前のコマンドが成功したか一目で分かる（`echo $?` 不要）。

---

## キーバインド

### カーソル移動
| キー | 動作 |
| --- | --- |
| `Ctrl+A` / `Home` | 行頭へ |
| `Ctrl+E` / `End` | 行末へ |
| `Ctrl+B` / `←` | 1 文字左へ |
| `Ctrl+F` / `→` | 1 文字右へ（行末ならゴースト補完を受け入れる） |
| `Ctrl+←` / `Ctrl+→` | 単語または `/` 単位で移動 |

### 編集
| キー | 動作 |
| --- | --- |
| `Backspace` / `Ctrl+H` | カーソル直前を削除 |
| `Delete` / `Ctrl+D` | カーソル位置を削除（空行での `Ctrl+D` は終了） |
| `Ctrl+W` | 単語または `/` 単位で直前を削除 |
| `Ctrl+U` | カーソルより前をすべて削除 |
| `Ctrl+K` | カーソル以降をすべて削除 |
| `Ctrl+L` | 画面をクリアして再描画 |
| `Alt+.` | 直前コマンドの **最終引数** を挿入 |
| `Ctrl+C` | 入力を破棄（[シグナル](#シグナル--ctrlc)参照） |
| `Ctrl+Z`（外部コマンド実行中） | 実行中のジョブを停止（[ジョブ制御](#ジョブ制御)参照） |

### 補完・履歴
| キー | 動作 |
| --- | --- |
| `Tab` | 補完（[Tab 補完](#tab-補完)） |
| `Ctrl+P` / `↑` | 履歴を 1 つ前へ |
| `Ctrl+N` / `↓` | 履歴を 1 つ後へ |
| `Ctrl+R` | 全履歴をインクリメンタル検索（[ピッカー](#ピッカーctrlr--ctrlt--ctrlg)） |
| `Ctrl+T` | カレント以下のファイル/ディレクトリを検索して挿入 |
| `Ctrl+G` | [自動記録したパス](#パスの自動記録)から選択して挿入 |
| `→`（行末） | ゴースト補完を受け入れる |

---

## Tab 補完

カーソル直前の状況から補完種別を判定する。

| 種別 | 条件 | 候補 |
| --- | --- | --- |
| 環境変数 | トークンが `$` で始まる | `$` から始まる環境変数名（前方一致） |
| コマンド名 | 第 1 トークンで `/` を含まない | `$PATH` 上の実行ファイル＋ビルトイン名（前方一致） |
| パス | 上記以外（第 2 トークン以降、または `/` を含む第 1 トークン） | ファイル/ディレクトリ |

- **第 1 トークンでも `/` を含めばパス補完**になる（`./script`, `../x`, `sub/cmd`, `/abs/x`, `~/x`）。シェルと同じく「スラッシュを含む語は PATH 検索しない」挙動。
- `cd` の引数のときは **ディレクトリのみ** を候補にする。
- **パス補完は部分一致（大小無視）**。例: `ls init<Tab>` は `my_init.bash` にマッチする。

確定の流れ:
1. 候補が **1 つ** → 即確定。
2. 候補が複数でも **共通接頭辞が現在トークンより長ければ** そこまで補完（次の Tab でメニュー）。
3. それ以外 → **グリッドメニュー**（fish 風の多列表示）。

### グリッドメニューの操作
| キー | 動作 |
| --- | --- |
| `Tab` / `→` | 次の候補 |
| `Shift+Tab` / `←` | 前の候補 |
| `↓` / `↑` | 同じ列の次/前の行 |
| `Enter` | 確定 |
| `Esc` | キャンセル |
| `Ctrl+C` | 中断 |
| 文字入力 | メニューを閉じてその文字を挿入 |

選択中の候補は白背景＋黒文字（iceberg トーンの淡色）で強調される。

---

## ゴースト補完

- 行末にいるときだけ、**履歴ベースのインライン候補**を薄く表示する。
- `→`（行末）または `Ctrl+F`（行末）で受け入れる。
- 候補の優先順位:
  1. **現在の cwd** で実行したコマンド（新しい順）
  2. 他ディレクトリで実行したコマンド（新しい順）
  - 同一コマンド文字列は最新のみ（重複排除）。前方一致。

---

## ピッカー（Ctrl+R / Ctrl+T / Ctrl+G）

外部バイナリ不要の自前ピッカー（fzf 相当を本体プロセス内で実装）。共通のマッチ仕様と操作キーを持つ。

### マッチ仕様（共通）
- クエリを **空白区切りのワード列**として扱う。
- 各ワードを **連続部分一致・左→右の順序保持・大小無視** で AND 検索する。
  - 例: `AAA CCC` は `/work/AAAAA/BBBB/CCCC` に一致するが、`CCC AAA` は順序が逆なので **不一致**。
- 並び順（スコア）:
  - **短いパスほど上位**（パス長ペナルティ）。
  - 末尾（basename）にかかるマッチを **加点**。
  - 同点は **MRU（直近に使った順）**。
- クエリが空のときは入力順（= MRU 順）のまま表示。

### 操作キー（共通）
| キー | 動作 |
| --- | --- |
| 文字入力 | クエリに追加して絞り込み |
| `Backspace` | クエリを 1 文字削除 |
| `↑` / `Ctrl+P` | 上の候補へ |
| `↓` / `Ctrl+N` | 下の候補へ |
| `Enter` | 確定 |
| `Esc` | 取り消し |
| `Ctrl+C` / `Ctrl+G` | 中断 |

### Ctrl+R — 履歴検索
- 全履歴を対象に検索。入力中の行を初期クエリとして渡す。

### Ctrl+T — ファイル/ディレクトリ検索
- カレント以下を **ストリーミング列挙**（巨大ツリーでも列挙の途中から検索・表示でき、`Ctrl+C`/`Esc` で即中断可）。
- 最大深さ 5。隠しファイル/ディレクトリ（`.` 始まり）は除外。
- 行頭コマンドが `cd` のときは **ディレクトリのみ**。
- 現在トークンに `/` が含まれるときは、その **ディレクトリをルート**にして、残りを初期クエリにする。
- **入力が空のときは cd モード**: ディレクトリのみを列挙し、選択結果を `cd <PATH>` として挿入する。

### Ctrl+G — 自動記録パス
- [自動記録したパス](#パスの自動記録)（MRU 順）から選ぶ。
- **入力が空のときは cd モード**: 候補をディレクトリのみに絞り、選択結果を `cd <PATH>` として挿入する。
- 入力があるときはファイル・ディレクトリ両方が対象。

---

## パスの自動記録

`reg_path` のような手動登録は廃止し、**コマンド引数に現れた実在パスを自動で記録**する。`Ctrl+G` の候補になる。

- 記録のタイミング: コマンド実行時（インタラクティブ入力のみ。RC ファイル実行は対象外）。
- 記録対象: 引数のうち **実在するパス**。`-` で始まるフラグや存在しないパスは無視。
  - **ファイルはファイル、ディレクトリはディレクトリ**として記録する。
- 相対パスは **実行前の cwd** を基準に解決するので、`cd foo` の `foo` も正しく記録される。
- 並びは **MRU**（使うたび先頭へ）。上限 500 件で、超えた古いものから捨てる。
- 永続化は `~/.my_shell_paths`。起動時に **存在しなくなったパスは自動的に除去**される。

---

## ビルトインコマンド

| コマンド | 説明 |
| --- | --- |
| `cd [DIR]` | ディレクトリ移動。引数なしで `$HOME`、`cd -` で `$OLDPWD`。移動前のディレクトリをスタックに積む（pushd 相当） |
| `popd` | ディレクトリスタックを 1 つ戻る |
| `abbr FROM TO` | 略語を定義 |
| `alias FROM TO` | エイリアスを定義 |
| `set VAR VAL` / `setenv VAR VAL` | 環境変数を設定（`setenv` は `set` の別名） |
| `jobs` | ジョブ一覧を表示（[ジョブ制御](#ジョブ制御)） |
| `fg [ID]` | ジョブをフォアグラウンドで再開（`ID` 省略時はピッカー） |
| `bg [ID]` | ジョブをバックグラウンドで再開（`ID` 省略時はピッカー） |

補足:
- `cd` は移動に成功したときだけ `OLDPWD` とスタックを更新する（存在しないディレクトリへの `cd` で壊れない）。
- ビルトインは `sh` を経由しないため、引数の `~`/`$VAR` は本体側が展開する。
- `fg`/`bg` の `ID` は `%1` 形式でも指定できる。

---

## abbr と alias の違い

| | abbr | alias |
| --- | --- | --- |
| 展開タイミング | Space／実行時にその場で展開し、**入力として見える**（fish 風の視覚展開） | 表示は変えず、**実行時に先頭トークンだけ**置き換える |
| 対象位置 | 先頭トークンのみ | コマンド位置すべて（パイプ後なども `sh` が展開） |

例（`~/.my_shell_rc` に記述）:
```sh
abbr gs git status      # `gs ` と打つと画面上で `git status ` に展開される
alias ll ls -la         # `ll` は実行時に `ls -la` として実行される
```

---

## 展開

- `~` / `~/...`: ホームディレクトリ（ビルトイン引数で展開）。
- `$VAR` / `${VAR}` / `$$` / `$?`: 環境変数 / PID / 直前の終了ステータス。
  - **外部コマンドでは `sh` 側**が、**ビルトイン引数では本体**が展開する。
- コマンド置換 `$(...)` は外部コマンド実行時に `sh` が解釈する。
  例: `vim $(which foo)` / `cd $(git rev-parse --show-toplevel)`。

---

## 設定ファイル

| ファイル | 役割 |
| --- | --- |
| `~/.my_shell_rc` | 起動時に 1 行ずつ実行する設定ファイル（`#` 始まりはコメント） |
| `~/.my_shell_history` | コマンド履歴（タブ区切り `時刻\tcwd\tコマンド`、最大 10,000 件） |
| `~/.my_shell_paths` | コマンド引数から自動記録したパス一覧（MRU 順、`Ctrl+G` で参照） |

`~/.my_shell_rc` の例は [`example/.my_shell_rc`](example/.my_shell_rc) を参照。abbr/alias/set はここに書いて永続化する。

---

## シグナル / Ctrl+C

- **入力中**の `Ctrl+C`: 入力行を破棄して新しいプロンプトを出す（シェル自体は終了しない）。
- **外部コマンド実行中**の `Ctrl+C`: 実行中の子プロセスにだけ `SIGINT` が届く。
  - 本体は `SIGINT` を無視（`SIG_IGN`）し、子プロセスでだけデフォルト動作（`SIG_DFL`）に戻している。これにより外部コマンドを止めてもシェルは生き残る。

---

## ジョブ制御

`Ctrl+Z` で実行中のコマンドを停止し、`fg`/`bg` で再開できる。vim を `:wq` せずに一時退避→作業→復帰、という使い方ができる。

### しくみ
- 外部コマンドは**独立したプロセスグループ**で起動し、実行中だけ端末の所有権をそのグループへ渡す（`tcsetpgrp`）。これにより `Ctrl+Z`（`SIGTSTP`）は**実行中の子だけ**に届き、シェル本体は止まらない。
- 本体は `SIGTSTP`/`SIGTTOU`/`SIGTTIN` を無視し、子は exec 前にこれらを既定動作へ戻す。
- 停止を検知すると `[1] Stopped  cmd` と表示してプロンプトに戻る。

### 操作
| 操作 | 動作 |
| --- | --- |
| `Ctrl+Z`（実行中） | フォアグラウンドのジョブを停止 |
| `jobs` | ジョブ一覧（`[ID] State  cmd`） |
| `fg [ID または タグ]` | フォアグラウンドで再開 |
| `bg [ID または タグ]` | バックグラウンドで再開 |

`fg`/`bg` の引数解決はタイプ数を抑えるよう次の順で動く（`bg` も同じ）:
1. **ジョブが 1 つだけなら即確定**（引数なしでも `fg` だけで再開）。
2. 引数が `1` / `%1` のような**番号**ならそれを直接使う。
3. それ以外の引数は**タグ（検索語）**として扱い、ピッカーをその語で絞り込んだ状態で開く。**1 件に絞れればピッカーを出さず即確定**（例: `fg vim`）。
4. 引数なしで複数あれば**ピッカー**を開く。候補は `[ID] State  cmd` 形式でコマンド名から識別でき、その場で検索もできる。
- バックグラウンドのジョブが完了・停止すると、次のプロンプト表示前に `[ID] Done  cmd` / `[ID] Stopped  cmd` と通知する。
- 停止ジョブが残った状態で `Ctrl+D` すると一度警告し、もう一度で `SIGHUP` を送って終了する。

### 制限
- `&` でのバックグラウンド実行は従来どおり `sh` 任せで、ジョブ表には**載らない**（`fg`/`bg`/`jobs` の対象外）。追跡するのは「`Ctrl+Z` で停止したジョブ」と「それを `bg` で再開したもの」だけ。

---

## 設計方針：あえて入れていない機能

軽量・低認知コストを優先し、次の機能は**意図的に入れていない**。

- 入力行のシンタックスハイライト
- auto-cd（ディレクトリ名だけで cd）
- コマンド実行時間のプロンプト表示（必要なら `time`）
- サブコマンド/フラグの事前定義補完（git/cargo など）
- 出力の常時キャプチャ / transcript

代替手段:
- 実行ログの検索 → 端末エミュレータの検索機能を使う。
- 出力の再利用 → `$(...)` と abbr で対応する。
- 簡単な `cd` → `Ctrl+G` / `Ctrl+T`（入力が空のとき `cd ` を補完）と abbr。
